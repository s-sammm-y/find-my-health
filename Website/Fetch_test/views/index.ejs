<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PatientTracker</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/styles/index.css">
    <link rel="stylesheet" href="/styles/entry_form.css">
</head>
<body>
    <main>
    <div class="background">
<% if(bool === true){ %>
    <h2>There is no bookings yet.</h2>
  <% } else {%>  
    <div class="lcontainer">
      <table class="table table-striped">
        <thead>
          <tr>
            <th scope="col">#</th>
            <th scope="col">Patient Name</th>
            <th scope="col">Gender</th>
            <th scope="col">OPD</th>
            <th scope="col">Arrival</th>
          </tr>
        </thead>
        <tbody>
        <% for(var i=0;i<data.length;i++) { %>  
          <tr>
            <td><%= data[i].booking_id %></td>
            <td><%= data[i].name %></td>
            <td><%= data[i].gender %></td>
            <td><%= data[i].opd%></td>
            <td><form class = "sendToQueue" action="/sendToQueue" method="post">
                <input type="hidden" class="form-control" id="booking_id" name="booking_id" value="<%= data[i].booking_id %>">
                <button type="submit" class="btn btn-primary" >Arrived</button>
            </form></td>
          </tr>
        <% } %>
        </tbody>
      </table>
    </div>
    <div class="rcontainer">
      <table class="table table-striped">
              <thead>
                <tr>
                  <th>Queue No</th>
                  <th>Token</th>
                  <th>Status</th>
                  <th>Wait time</th>
                </tr>
              </thead>
              <% if (data2 && data2.length) { %> 
              <tbody>
                <% for (var i = 0; i < data2.length; i++) { %>  
                  <tr>
                    <td><%= data2[i].queue_no %></td>
                    <td><%= data2[i].token %></td>
                    <td><%= data2[i].status %></td>
                    <td>const</td>
                  </tr>
                <% } %>
              </tbody>
              <% } %>
            </table>
    </div>
<% } %>
</div>
</main>
<footer>
  <div class="footer"></div>
</footer>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>  